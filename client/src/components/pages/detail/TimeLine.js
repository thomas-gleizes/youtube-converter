import React from "react";

import Card from "../../layouts/Card";

const TimeLine = ({ video }) => {
  return (
    <Card title="Timeline">
      <div></div>
    </Card>
  );
};

export default TimeLine;
